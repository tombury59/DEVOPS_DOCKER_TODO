

apiVersion: v1
kind: Service
metadata:
  name: tasks-service
  labels:
    app: tasks-service
spec:
  type: NodePort
  selector:
    app: tasks-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
